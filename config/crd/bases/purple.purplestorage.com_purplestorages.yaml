---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: purplestorages.purple.purplestorage.com
spec:
  group: purple.purplestorage.com
  names:
    kind: PurpleStorage
    listKind: PurpleStorageList
    plural: purplestorages
    singular: purplestorage
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PurpleStorage is the Schema for the purplestorages API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PurpleStorageSpec defines the desired state of PurpleStorage
            properties:
              ibm_spectrum_scale_container_native_version:
                description: Foo is an example field of PurpleStorage. Edit purplestorage_types.go
                  to remove/update
                type: string
              machineconfig:
                properties:
                  labels:
                    additionalProperties:
                      type: string
                    type: object
                type: object
              pull_secret:
                type: string
            type: object
          status:
            description: PurpleStorageStatus defines the observed state of PurpleStorage
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
